// Where the app starts on a phone. Expo hands the App component to React Native, whether this is a build installed
// from an APK, Expo Go, or the web preview.
import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);
